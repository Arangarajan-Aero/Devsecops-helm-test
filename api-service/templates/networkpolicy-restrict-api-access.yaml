apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: restrict-api-access
  namespace: dev
spec:
  podSelector:
    matchLabels:
      app: api
  ingress:
  - from:
    - podSelector:
        matchLabels:
          app: web
    - podSelector:
        matchLabels:
          app: db
  policyTypes:
  - Ingress
  egress:
  - to:
    - podSelector:
        matchLabels:
          app: db
    - podSelector:
        matchLabels:
          app: web
  policyTypes:
  - Egress
